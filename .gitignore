# Dependencies / build output
node_modules/
.next/
out/
coverage/
playwright-report/
test-results/
*.tsbuildinfo

# Cloudflare OpenNext / Wrangler local build artifacts (do not commit)
.open-next/
.wrangler/

# OS / editor / logs
.DS_Store
*.log

# Environment files — never commit real secrets
.env
.env.local
.env.*
!.env.example

# Local agent execution evidence — useful while running /ship, not public portfolio content
.opencode/reports/

# Local visual-QA artifacts (generated by scripts/visual-qa.mjs)
.visual-qa/

# Private sales operations material — never commit to the public repo.
# These live in the sibling `freelance-sales-ops/` directory.
data/
profiles/
templates/
prospects/
opportunities/
outreach/
pipeline/
leads/
proposals/
sales-ops/
finance/
strategy/

# Credentials and key material
*.pem
*.key
*.p12
*.pfx
